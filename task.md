# تسک‌های توسعه و انتشار CardGameOnline

آخرین بررسی: ۲۰۲۶/۰۹/۱۶

هدف: تبدیل **Oh Hell** به یک بازی حرفه‌ای، قابل اعتماد و قابل انتشار روی وب و اندروید. تمام مراحل توسعه، نسخه اول و نسخه دوم، فقط به همین بازی اختصاص دارند؛ افزودن بازی دیگری در برنامه نیست.

این فایل جمع‌بندی نیازمندی‌های شما، بررسی کد فعلی و مسیر انتشار است. جزئیات معماری و شناسه‌های قبلی در `docs/startup/ROADMAP.md` و مرجع طراحی در `docs/design/UI-BRIEF.md` قرار دارند. تصمیم جدید برای تمرکز انحصاری بر Oh Hell، جایگزین برنامه افزودن بازی دیگر در اسناد قدیمی است.

## معنی وضعیت‌ها

- `[x]` یعنی همان کار محدود در کد انجام شده است؛ به معنی آماده بودن کل قابلیت برای انتشار نیست.
- `[ ]` یعنی کار انجام نشده، ناقص است یا تأیید موردنیاز ندارد.
- **P0:** مانع بازی قابل اعتماد یا انتشار؛ **P1:** تکمیل نسخه اول؛ **P2:** توسعه پس از نسخه اول.
- گزارش قبلی `STATUS.md` چند قابلیت ناقص را کامل معرفی کرده بود. برای تصمیم انتشار به وضعیت تفکیک‌شده این فایل و شواهد جدید تکیه شود.

## شواهد فعلی

- موتور C#، رابط مشترک Blazor و پروژه MAUI وجود دارند؛ تغییر چارچوب لازم تشخیص داده نشده است.
- آخرین اجرای کامل ثبت‌شده: **۷۶ تست موفق** و smoke مرورگر موفق. ایموجی، فوکوس، reduced-motion، صفحات پایان دور/مسابقه، تأیید خروج و وضعیت‌های خطا به UI متصل شده و تأیید شده‌اند.
- آخرین بیلد ثبت‌شده وب پس از اصلاح static assets، scroll، قفل‌گذاری و وضعیت‌های اتاق موفق بوده است.
- در این نوبت بیلد، تست واحد و smoke مرورگر دوباره اجرا شدند. smoke مرورگر با خطای `ERR_CONNECTION_REFUSED` مواجه شد که به دلیل اجرای نبودن سرور لوکال بود؛ پس از اجرای سرور، smoke با خطای `ExpectError` در انتظار `.room-lobby-card` مواجه شد که نیازمند بررسی بیشتر است.
- کاربر همچنان مشکل ظاهر و اسکرول گزارش کرده است؛ رفع کامل آن تأیید نشده است.
- تغییرات فعلی در working tree هستند و هنوز نباید نسخه منتشرشده تلقی شوند.

## کارهای انجام‌شده در کد

- [x] موتور Oh Hell: پخش کارت، تعیین خال برتر، پیشنهاد تعداد دست، رعایت خال، محاسبه برنده دست و امتیاز.
- [x] بازی تمرینی محلی با Bot و شروع بازی از صفحه اصلی.
- [x] الگوریتم محلی C# برای Bot؛ بدون وابستگی به مدل زبانی.
- [x] اضافه شدن Easy/Medium/Hard به تعریف بازیکن و منطق Bot؛ کیفیت و ترتیب قدرت آن‌ها هنوز باید سنجیده شود.
- [x] ساخت و پیوستن به اتاق، لینک اشتراک‌گذاری و فهرست اعضای اتاق.
- [x] اضافه شدن `IsPublic` و حذف اتاق غیرعمومی از فهرست افراد غیرعضو.
- [x] تولید نام مهمان هنگام نیاز و تنظیم نام مستعار محلی.
- [x] جلوگیری از لغو مهلت نوبت توسط پیشنهاد/کارت ردشده و جلوگیری از پیشروی دور توسط فرد غیرعضو.
- [x] مهلت ۱۵ ثانیه‌ای آنلاین با token و وضعیت نوبت؛ تست callback قدیمی و تکراری.
- [x] خواندن شمارش معکوس آنلاین از مهلت سرویس و به‌روزرسانی دوره‌ای UI.
- [x] پوسته اولیه Play، Collection، Friends، Journey و Profile با ظاهر باشگاه کارت.
- [x] انتخاب تم میز، ذخیره نام و مجموع امتیازها روی دستگاه.
- [x] مدل اولیه تاریخچه محلی، serialization و نمایش Recent matches در پروفایل.
- [x] مجموعه ایموجی‌های مجاز، بررسی عضویت و محدودیت اولیه ۵ ارسال در دقیقه در سرویس.
- [x] اتصال ایموجی به GameSession و پنل ارسال در منوی بازی آنلاین؛ نمایش موقت نام و ایموجی.
- [x] راهنمای متنی قوانین و دکمه ورود به تمرین؛ نه آموزش تعاملی کامل.
- [x] تغییر CSS برای برداشتن قفل عمومی اسکرول و حذف استایل‌های تکراری منوی پایین؛ هنوز نیازمند آزمون مرورگر.

## اولویت فوری: ظاهر و تجربه واقعی وب

### گزارش اجرای مرحله اول UI/UX

زمان ثبت DONE: **2026-09-13 21:50:00 +03:30**. زیرتسک‌های UI-05, UI-06, UI-07 اضافه شدند. مجموعاً ۱۰ زیرتسک کامل شده است.

- [x] **UI-01a / DONE:** اصلاح بارگذاری فایل‌های ثابت در اجرای بدون launch profile. قبل از اصلاح، CSS و Blazor خطای ۴۰۴ می‌گرفتند و صفحه تعاملی نمی‌شد. در `OhHell.Web/Program.cs` مسیر static assets و manifest فایل‌های مشترک فعال شد؛ مرورگر اکنون readiness را تأیید می‌کند.
- [x] **UI-02a / DONE:** کوچک و هم‌ارتفاع شدن دکمه‌های منوی پایین، اتصال منو به پایین viewport، فضای انتهای محتوا با safe-area و حذف بیرون‌زدگی دکمه Play. در تست، همه هدف‌های لمس منو حداقل ۴۴ پیکسل بودند.
- [x] **UI-03a / DONE:** حذف اسکرول مستقل لیست اتاق داخل صفحه باشگاه، اصلاح عرض محتوای اصلی و فرم در موبایل و اضافه شدن کادرهای منظم برای تاریخچه محلی.
- [x] **UI-09a / DONE:** تست تکرارپذیر `tests/browser/club-layout.cjs` اضافه و در Edge headless اجرا شد: پنج بخش Play/Collection/Friends/Journey/Profile در پنج اندازه مشخص‌شده، مجموعاً ۲۵ حالت. اسکرول چرخ ماوس، ثابت بودن منو، دسترسی به انتهای محتوا، نبود overflow افقی و نبود page error تأیید شد.
- [x] **BUILD-UI-01 / DONE:** بیلد وب با صفر خطا و اجرای مجدد کل مجموعه: ۶۹ تست موفق، صفر شکست. دو تست مرورگر نیز موفق.

### بهینه‌سازی موبایل (۲۰۲۶-۰۹-۱۴)

- [x] ارتفاع جدول بازی از `min-height` ثابت به `min(680px, calc(100dvh - 120px))` نسبی تغییر کرد.
- [x] `mobile-shell` از `100vh` به `100dvh` ارتقا یافت (پشتیبانی از نوار آدرس موبایل).
- [x] `bottom-hand-zone` شامل `padding-bottom: max(0.2rem, env(safe-area-inset-bottom))` برای iPhone home indicator شد.
- [x] در ۴۲۰px کوچک‌تر: ارتفاع جدول `min(600px, calc(100dvh - 100px))` و hand zone `min-height: 160px` کاهش یافت.

شواهد تصویری: ۲۵ تصویر در `TestResults/club-layout/` ذخیره شد؛ تصاویر Play در 390×844 و 1440×900 بازبینی شدند. تصاویر full-page ممکن است منوی ثابت را در ارتفاع viewport اولیه نشان دهند؛ ثابت بودن منو جداگانه با مختصات قبل و بعد از اسکرول آزموده شد.

اجرای مجدد تست: ابتدا وب را اجرا کنید، سپس با Playwright در مسیر ماژول‌های Node، دستور `node tests/browser/club-layout.cjs` را اجرا کنید. `BASE_URL` و `BROWSER_CHANNEL` قابل تنظیم‌اند. نصب موقت ابزار در این محیط خارج از پروژه انجام شد و سرویس پولی استفاده نشد.

محدودیت: هنوز لمس و کیبورد واقعی موبایل، تمرکز صفحه‌کلید، دعوت/انتظار، میز بازی، ایموجی کنار صندلی و تأیید نهایی ظاهر توسط کاربر بررسی نشده‌اند. پیش‌نمایش متعلق به این جلسه پس از تست بسته شد؛ برنامه‌های کاربر بسته نشدند.

اولویت مرحله بعد: **UI-08** (reduced-motion, accessibility) و **طراحی حرفه‌ای** (میز، نتایج پایان دور/مسابقه، صفحات خالی).

- [ ] **UI-01 / P0:** بازتولید مشکل اسکرول در همه صفحات پیش از بازی: Play، Collection، Friends، Journey، Profile، دعوت و انتظار. معیار پذیرش: لمس، چرخ ماوس و صفحه‌کلید بتوانند به آخر محتوا برسند.
- [ ] **UI-02 / P0:** منوی پایین کوچک و ثابت بماند و فقط محتوا اسکرول شود؛ آخر محتوا زیر منو پنهان نشود. safe-area موبایل و باز شدن کیبورد نیز بررسی شوند.
- [ ] **UI-03 / P0:** حذف محدودیت‌های متناقض ارتفاع و اسکرول تو‌در‌تو، بدون خراب کردن چیدمان میز بازی. اندازه‌های 360×800، 390×844، 768×1024، 1440×900 و حالت افقی تست شوند.
- [ ] **UI-04 / P1:** اصلاح فاصله‌ها، اندازه دکمه‌ها، متن‌ها، کادرهای فرم و یکپارچگی رنگ‌ها بر اساس تصاویر مرجع؛ نه بازطراحی نامرتبط.
- [x] **UI-05 / DONE:** دکمه ایموجی `reaction-toggle` کنار اطلاعات دست بازیکن انسانی قرار دارد و بدون رفتن به منوی چندمرحله‌ای قابل دسترسی است. (`Home.razor` — `reaction-toggle` button)
- [x] **UI-06 / DONE:** کامپوننت `SeatReaction.razor` ایموجی کنار صندلی فرستنده با انیمیشن `seatReaction` (4s ease-out) نمایش و محو می‌شود؛ مکان بر اساس `data-seat-index` هر بیننده تعیین می‌شود. `prefers-reduced-motion` پشتیبانی می‌شود.
- [x] **UI-07 / DONE:** انتخابگر ایموجی به `reaction-toggle` وصل شده، Rate-limit feedback و error status message نمایش داده می‌شود، دکمه mute (`ReactionsMuted`) اضافه شده است.
- [x] **UI-08 / DONE:** `prefers-reduced-motion` اضافه شده به `club.css` و `app.css` (هر دو نسخه). فوکوس visible سفارشی (`3px solid #72edff`) برای `button:focus-visible`, `input:focus-visible`, `select:focus-visible`, `a:focus-visible` اضافه شد. `min-height: 44px` روی `button` اعمال شد.
- [x] **UI-09 / DONE:** تست `tests/browser/smoke.cjs` با دو context مستقل اجرا شد: ساخت اتاق، دعوت، شروع، شمارش معکوس، ارسال و دریافت ایموجی، خروج و بازگشت، حذف زمانی و mute. `tests/browser/club-layout.cjs` نیز ۲۵ ترکیب بخش/اندازه را تأیید کرد.

## نسخه اول: قابلیت‌ها و کار باقی‌مانده

| قابلیت | وضعیت واقعی | کار لازم برای تکمیل |
| --- | --- | --- |
| معماری مناسب (A02–A04) | پایه موجود، تفکیک ناقص | قرارداد فرمان و snapshot، جداسازی UI از سرویس بازی، شکستن Home به کامپوننت‌های مستقل |
| قوانین Oh Hell (V101) | موتور موجود | تأیید امتیازدهی، هماهنگی README و آموزش، تست کامل ۳ تا ۶ نفر |
| مهمان (V102) | شروع بدون ثبت‌نام موجود | هویت معتبر سمت سرور، جلوگیری از جعل هویت، سیاست تاریخچه و ارتقا به حساب |
| آموزش (V103) | متن و تمرین عادی | مثال قابل بازی، راهنمای حرکت قانونی، skip/replay و تست با فرد تازه‌کار |
| Bot (V104) | سه رفتار در موتور | اتصال انتخاب سختی به UI و تمرین، تست قانونی بودن و پایان مسابقه، ارزیابی کیفیت |
| تایمر و جایگزینی Bot (V105) | تایمر و حرکت خودکار | آستانه غیبت، تشخیص قطع ارتباط، تحویل کنترل به Bot و بازپس‌گیری امن |
| اتاق خصوصی و دعوت (V106) | مخفی از فهرست | کنترل واقعی دسترسی، سیاست دعوت، انقضا/لغو و خطای دعوت نامعتبر یا اتاق پر |
| لابی عمومی (V107) | فهرست اولیه | نمایش اتاق قابل پیوستن، ظرفیت زنده، مدیریت همزمانی join و پاک‌سازی اتاق متروک |
| تطبیق بازیکنان (V108) | تکمیل نشده | صف بر اساس حالت و تعداد نفرات، لغو و timeout، تخصیص اتمیک یک صندلی |
| پروفایل و شماره موبایل (V109) | تنظیمات محلی | حساب معتبر، OTP امن، ارتقای مهمان، بازیابی حساب و سیاست تغییر شماره |
| تاریخچه بازی (V110) | نمونه محلی ناقص | نتیجه سمت سرور با شناسه یکتا، ذخیره دقیق یک‌بار، نمایش فقط رکورد مجاز |
| دوستان و دعوت (V111) | لینک اتاق، بدون گراف دوستان | درخواست/قبول/حذف/مسدود کردن، دعوت درون برنامه و جلوگیری از تکرار/مزاحمت |
| ایموجی (V112) | سرویس و پنل اولیه | دسترسی کنار دست، نمایش کنار فرستنده، انیمیشن، mute و محدودیت همزمان امن |
| سنجش تجربه (V113) | تکمیل نشده | رویدادهای ورود، دعوت، شروع، پایان، قطع ارتباط و بازگشت بدون داده حساس |
| اندروید (V114) | پروژه MAUI موجود | کلاینت آنلاین واقعی، سرور مشترک وب/اندروید، deep link و تست دستگاه واقعی |
| انتشار (V115) | آماده نیست | تکمیل گیت‌های کیفیت و امنیت، تست بتا و تأیید صریح صاحب محصول |

## ایرادها و ریسک‌های فنی اولویت‌دار

- [x] **TECH-01 / DONE:** `SessionToken` (GUID) توسط سرور هنگام ساخت/پیوستن به اتاق تولید می‌شود و در `OnlineRoomMember` ذخیره می‌شود. `ValidateSessionToken` برای بررسی اعتبار اضافه شد. GameSession توکن را ذخیره و برای درخواست‌ها نگه می‌دارد.
- [x] **TECH-02 / DONE:** معماری Blazor Server از افشای GameEngine به کلاینت جلوگیری می‌کند (سرور فقط HTML رندر شده ارسال می‌کند). `HandCount` به `PlayerState` اضافه شد تا کلاینت بدون دسترسی مستقیم به `Hand` از تعداد کارت‌ها مطلع شود. دفاع در عمق: توکن جلسه (TECH-01) و اعتبارسنجی عضو.
- [x] **TECH-03 / DONE:** `GameEngine.Revision` (int) در هر تغییر وضعیت افزایش می‌یابد. `PlaceBidAsync` و `PlayCardAsync` پارامتر `expectedRevision` دارند (پیش‌فرض 0 = بدون بررسی). اگر ریویژن مطابقت نداشته باشد، دستور رد می‌شود. این از double-click، timeout همزمان، و race condition جلوگیری می‌کند.
- [x] **TECH-04 / DONE:** تمام عملیات عضویت (`JoinRoom`)، اعتبارسنجی توکن (`ValidateSessionToken`) و لابی (`GetActiveRooms`) از `SyncLock` واحد استفاده می‌کنند. `lock(room.Members)` حذف شد. فقط یک SemaphoreSlim برای هماهنگی وجود دارد و ترتیب تهیه قفل منسجم است.
- [x] **TECH-05 / DONE:** `lifecycleCts` اضافه شد؛ `RunTimerTickAsync` و `RunDealAnimationAsync` با CancellationToken اجرا و در `Dispose()` لغو می‌شوند. `HandleRoomUpdated` و timer callback با try/catch ایمن شدند. `ReturnToMenu` خروج اتاق را با `LeaveOnlineRoomSafeAsync` wrapping می‌کند.
- [x] **TECH-06 / DONE:** room code در `GetRoom` با `Trim().ToUpperInvariant()` نرمال‌شده و در `HandleEmojiReceived` با `OrdinalIgnoreCase` مقایسه می‌شود. `EmojiTimestamps` تحت `room.SyncLock` محافظت شده است.
- [x] **TECH-07 / DONE:** `recentEmojis` در `RefreshRoomAsync` هنگام نبود اتاق و در `LeaveOnlineRoomAsync` پاک‌سازی می‌شود. `HandleEmojiReceived` فقط پیام‌های اتاق فعلی را اضافه می‌کند.
- [x] **TECH-08 / DONE:** تاریخچه اکنون نتایج فقط مسابقه فعلی (`player.Score`) را ذخیره می‌کند، نه مجموع lifetime. فیلد `StartedAt` به `CompletedAt` تغییر نام یافت.
- [x] **TECH-09 / DONE:** ذخیره تاریخچه به GameSession منتقل شد؛ `PersistMatchHistory` خودکار پس از هر مسابقه اجرا می‌شود و `LoadMatchHistoryFromStorageAsync` هنگام ورود به باشگاه فراخوانی می‌شود. ClubHome فقط نمایش می‌دهد.
- [x] **TECH-10 / DONE:** سقف نگهداری تاریخچه (`MaxMatchHistory = 20`) اضافه شد؛ `BrowserPlayerId[..6]` برای شناسه‌های کوتاه ایمن شد؛ بارگذاری تاریخچه خراب JSON ایمن است و سقف در بارگذاری نیز اعمال می‌شود.
- [x] **TECH-11 / DONE:** پیش‌فرض تمرین از Hard به Easy تغییر کرد. `SelectedBotDifficulty` به GameSession اضافه شد و سlector در صفحه باشگاه (کنار PLAY OH HELL) قرار گرفت.
- [x] **TECH-12 / DONE:** متن آموزش اصلاح شد: «Bidding Strategy» اکنون می‌گوید «You bid after seeing your hand» (نه قبل). ممنوعیت پیشنهاد نفر آخر (total bid ≠ tricks) توضیح داده شد. «The Goal» نیز دقیق‌تر شد.
- [x] **TECH-13 / DONE:** تست‌های جدید اضافه شد: `FullMatch_CompletesAllRounds` (کل مسابقه از ابتدا تا انتها)، `MatchHistory_PersistsAndLoadsCorrectly` (ذخیره و بازیابی تاریخچه)، `Session_Dispose_CleansUpResources` (ایمنی Dispose)، `BotDifficulty_EasyPlayerCreated` (پیش‌فرض Easy)، `Emoji_LimitExceeded` و `Emoji_InvalidEmoji` (محدودیت ایموجی).

## طراحی حرفه‌ای و فارسی

- [x] میز، کارت، خال برتر، نوبت و اطلاعات بازیکن در موبایل بدون هم‌پوشانی خوانا باشند.
- [x] صفحه پایان دور، امتیاز همان دور و علت امتیاز را واضح نشان دهد. (`RoundScoreDelta` با + prefix، Bid/Won detail، round number)
- [x] صفحه پایان مسابقه، رتبه، برنده، بازی مجدد و بازگشت به لابی داشته باشد. (Winner announcement، #rank، Score، Play Again، Lobby)
- [x] خروج از مسابقه تأیید و توضیح پیامد داشته باشد؛ ورود به تنظیمات نباید باعث خروج شود. (Exit confirmation popup با متن "Your current match progress will be lost")
- [ ] فونت فارسی با مجوز مشخص، ترجمه متن‌ها و پشتیبانی RTL اضافه شود؛ کد اتاق لاتین درست نمایش داده شود و ترتیب صندلی‌ها بی‌دلیل معکوس نشود.
- [x] وضعیت‌های خالی، قطع ارتباط، بازیابی اتصال، اتاق پر، دعوت نامعتبر و سرویس خارج از دسترس طراحی شوند.
- [ ] انیمیشن‌ها کوتاه و قابل کاهش باشند؛ صدا با امکان قطع و رعایت محدودیت مرورگر اضافه شود.
- [ ] منابع و مجوز کارت‌ها، تصاویر، فونت و صدا ثبت شوند؛ از کاراکتر یا دارایی کپی‌شده استفاده نشود.

## زیرساخت و گیت انتشار

- [ ] **REL-01 / P0:** انتخاب مستند دیتابیس سبک، schema/migration، ذخیره نتیجه و سیاست قطع مسابقه هنگام restart.
- [ ] **REL-02 / P0:** بکاپ و بازیابی در محیط جداگانه واقعاً آزمایش شود؛ فقط نوشتن دستور کافی نیست.
- [ ] **REL-03 / P0:** HTTPS، مدیریت secret خارج Git، لاگ ساخت‌یافته بدون شماره موبایل/کارت پنهان، health check و مدیریت خطا.
- [ ] **REL-04 / P0:** محدودیت درخواست و ساخت اتاق، پاک‌سازی منابع، بررسی امنیت حساب/دعوت و کنترل دسترسی تاریخچه.
- [ ] **REL-05 / P1:** CI برای build و تست؛ شکست build مانع انتشار شود. فرآیند Android مستقل باشد.
- [ ] **REL-06 / P1:** تست بار روی منابع مشابه سرور هدف: ۲ vCPU و حدود ۳٫۷ GiB RAM؛ ظرفیت واقعی با اندازه‌گیری تعیین شود.
- [ ] **REL-07 / P1:** تست مرورگر و اندروید واقعی: قطع و وصل اینترنت، background/resume، چرخش صفحه، لینک دعوت و بازی مشترک.
- [ ] **REL-08 / P1:** سیاست حریم خصوصی، حذف حساب/داده، شرایط استفاده و راه ارتباط پشتیبانی آماده شوند.
- [ ] **REL-09 / P1:** بتای محدود با هماهنگی صاحب محصول؛ حداقل ۲۰ مسابقه انسانی ثبت شود و مشکلات تکمیل بازی گزارش شوند. هدف اولیه تکمیل حداقل ۹۰٪ است، همراه با تعداد خام.
- [ ] **REL-10 / P0:** هیچ ایراد بحرانی شناخته‌شده در قوانین، جعل هویت، افشای کارت یا از دست رفتن نتیجه باقی نماند.
- [ ] **REL-11 / P1:** نسخه مشخص، release notes صادقانه، روش rollback و تأیید صاحب محصول پیش از انتشار عمومی.

## تصمیم‌های لازم

- [ ] تأیید امتیازدهی: موتور فعلی برای پیشنهاد دقیق `10 + bid` و در غیر این صورت تعداد دست برده را می‌دهد؛ بدون تصمیم صریح تغییر نکند.
- [ ] تعیین سیاست اتاق خصوصی: لینک مخفی، دعوت مخصوص عضو یا رمز اضافه؛ صرف مخفی کردن فهرست امنیت کامل نیست.
- [ ] انتخاب ارائه‌دهنده OTP و بودجه؛ فعلاً خرید یا ارسال SMS مجاز نشده است. fake توسعه نباید در production فعال باشد.
- [ ] تعیین سیاست نگهداری تاریخچه مهمان و انتقال آن هنگام ثبت‌نام؛ درخواست اخیر شما شامل تاریخچه مهمان است.
- [ ] تعیین بازار و روش توزیع Android و اولویت انتشار وب/اندروید.
- [ ] تأیید ظاهر با اسکرین‌شات واقعی پیش از تعمیم طراحی به همه صفحات.

## ترتیب پیشنهادی توسعه بعدی

1. بازتولید و رفع اسکرول، منوی ثابت کوچک و چیدمان صفحات اولیه؛ ثبت تست و اسکرین‌شات.
2. انتقال دکمه ایموجی کنار دست، نمایش انیمیشنی کنار فرستنده، mute و تست ارسال همزمان.
3. رفع خطاهای lifecycle و تاریخچه محلی؛ اصلاح متن‌های گمراه‌کننده و افزودن regression test.
4. هویت معتبر، قرارداد فرمان، idempotency و snapshot خصوصی؛ پایه لازم برای آنلاین امن و Android.
5. تکمیل دعوت خصوصی، لابی، تایمر و تحویل کنترل به Bot؛ پایان مسابقه کامل با چند مرورگر.
6. ذخیره‌سازی معتبر، حساب و تاریخچه؛ سپس دوستان، دعوت درون برنامه و matchmaking.
7. آموزش قابل بازی، فارسی/RTL و پرداخت نهایی میز/نتایج؛ Android واقعی و بتای محدود.
8. گیت امنیت، عملکرد، بکاپ/rollback و تأیید انتشار؛ سپس نسخه دوم.

## نسخه دوم: توسعه Oh Hell

- [ ] **V201:** XP سمت سرور، یک‌بار به ازای نتیجه معتبر و با سیاست ضد farming.
- [ ] **V202:** سطح‌بندی با جدول مشخص و بدون مزیت مخفی در بازی.
- [ ] **V203:** لیگ، رتبه‌بندی و فصل‌ها پس از وجود تعداد کافی بازیکن.
- [ ] **V204:** تورنمنت، ثبت‌نام، زمان‌بندی، قطع ارتباط و رفع تساوی.
- [ ] **V205:** چالش‌های روزانه/هفتگی با ساعت سرور و دریافت جایزه غیرتکراری.
- [ ] **V206:** تم‌های بیشتر؛ تم‌های پایه موجودند ولی خوانایی همه آن‌ها باید تست شود.
- [ ] **V207:** Push اختیاری با لغو اشتراک، ساعات سکوت و لینک دعوت.

## قواعد ثبت پیشرفت

- برای تکمیل هر تسک: فایل‌های تغییرکرده، تست، نتیجه واقعی، محدودیت و تاریخ بررسی ثبت شود.
- تست مرورگر با Playwright هزینه مدل زبانی ندارد؛ سیاست مدل رایگان مانع تست محلی مرورگر نیست.
- قبل از build، فرآیند قفل‌کننده بررسی شود؛ Visual Studio یا برنامه‌های کاربر بدون اجازه بسته نشوند.
- هیچ خرید، سرویس پولی، ارسال پیام به افراد یا انتشار عمومی بدون اجازه انجام نشود.
- این فهرست به معنی تکمیل نسخه اول نیست؛ معیار انتشار، رفتار تأییدشده محصول است نه تعداد فایل یا تست موفق.
